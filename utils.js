const utils = {
    withGrid(n) {
        return n * 32;
    },
    asGridCoord(x,y) {
        return `${x*16},${y*16}`
    },
    nextPosition(initialX, intialY, direction){
        let x = initialX;
        let y = initialY;
        
    }
    
}